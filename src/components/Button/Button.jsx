 

const Button = ({ name }) => {
  return (
    <div>
        <button className="px-5 py-1 text-xs font-normal text-gray-800 bg-gray-100 rounded-lg ">{name}</button>
    </div>
  )
}

export default Button